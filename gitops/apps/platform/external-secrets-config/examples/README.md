# External Secrets Config Examples

These files show how to define a shared `ClusterSecretStore` named `platform-secrets` for different secret providers.

For ROSA Classic in this repo, AWS Secrets Manager is the default secret backend.

Copy one of these examples only if you are not using AWS Secrets Manager as the shared store.
